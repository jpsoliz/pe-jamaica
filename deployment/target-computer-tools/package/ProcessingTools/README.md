# Processing Tools

The C# add-in must call stable adapter or toolbox entrypoints from this package. It must not call legacy scripts directly.

Adapters normalize inputs, outputs, exit codes, logging, and contract artifact writing for preflight, extraction, validation, and output generation.
