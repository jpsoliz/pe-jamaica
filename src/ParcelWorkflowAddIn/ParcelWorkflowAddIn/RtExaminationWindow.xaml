<controls:ProWindow x:Class="ParcelWorkflowAddIn.RtExaminationWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
                    Title="RT Examination"
                    Width="1080"
                    Height="740"
                    MinWidth="920"
                    MinHeight="620"
                    WindowStartupLocation="CenterOwner">
  <controls:ProWindow.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    <Style x:Key="RtIconImageStyle" TargetType="Image">
      <Setter Property="Width" Value="16" />
      <Setter Property="Height" Value="16" />
      <Setter Property="Margin" Value="0,0,6,0" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
  </controls:ProWindow.Resources>
  <Grid Background="#F4F7F9">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="#EAF2F7" BorderBrush="#C8D6DF" BorderThickness="0,0,0,1" Padding="10,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <TextBlock FontWeight="SemiBold" Text="Transaction:" />
          <TextBlock Margin="6,0,16,0" Text="{Binding TransactionNumber}" />
          <TextBlock FontWeight="SemiBold" Text="Stage:" />
          <TextBlock Margin="6,0,16,0" Text="{Binding StageName}" />
          <TextBlock FontWeight="SemiBold" Text="Linked TR / Plan No.:" />
          <TextBlock Margin="6,0,16,0" Text="{Binding PePlanNumberText}" />
          <TextBlock FontWeight="SemiBold" Text="Originating PE Transaction:" />
          <TextBlock Margin="6,0,0,0" Text="{Binding OriginatingPeText}" />
        </StackPanel>
        <TextBlock Grid.Column="1" Foreground="#41515A" Text="Attribute review only" />
      </Grid>
    </Border>

    <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="0,0,0,1" Padding="10,6">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal">
          <Button Width="154" Height="28" Command="{Binding LoadLinkedPeDataCommand}">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Style="{StaticResource RtIconImageStyle}" Source="Images/RTIcons/LoadSpatialData.png" />
            <TextBlock VerticalAlignment="Center" Text="Load Spatial Data" />
          </StackPanel>
          </Button>
          <Button Width="86" Height="28" Margin="8,0,0,0" Command="{Binding SaveCommand}">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Style="{StaticResource RtIconImageStyle}" Source="Images/RTIcons/Save.png" />
            <TextBlock VerticalAlignment="Center" Text="Save" />
          </StackPanel>
          </Button>
          <TextBlock Margin="16,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Decision to:" />
          <ComboBox Width="220"
                  Height="28"
                  Margin="6,0,0,0"
                  DisplayMemberPath="Label"
                  ItemsSource="{Binding CompletionDecisions}"
                  SelectedItem="{Binding SelectedCompletionDecision, Mode=TwoWay}" />
          <Button Width="124" Height="28" Margin="8,0,0,0" Command="{Binding CompleteCommand}">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Style="{StaticResource RtIconImageStyle}" Source="Images/RTIcons/SaveClose.png" />
            <TextBlock VerticalAlignment="Center" Text="Next Stage" />
          </StackPanel>
          </Button>
        </StackPanel>
        <Button Grid.Column="1" Width="92" Height="28" Margin="8,0,0,0" Command="{Binding CancelCommand}">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Style="{StaticResource RtIconImageStyle}" Source="Images/RTIcons/Cancel.png" />
            <TextBlock VerticalAlignment="Center" Text="{Binding CloseActionText}" />
          </StackPanel>
        </Button>
      </Grid>
    </Border>

    <TabControl Grid.Row="2" Margin="8">
      <TabItem Header="Context">
        <Grid Margin="8">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8" Margin="0,8,0,8">
            <StackPanel>
              <TextBlock FontWeight="SemiBold" Text="Linked Transaction Context" />
              <TextBlock Margin="0,6,0,0" Text="{Binding StatusText}" TextWrapping="Wrap" Foreground="#41515A" />
            </StackPanel>
          </Border>
          <GroupBox Grid.Row="1" Header="Warnings">
            <ListBox ItemsSource="{Binding Warnings}" />
          </GroupBox>
        </Grid>
      </TabItem>

      <TabItem Header="Neighbors / Parties">
        <DataGrid Margin="8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  ItemsSource="{Binding PartyRows}">
          <DataGrid.Columns>
            <DataGridTemplateColumn Header="Role" Width="140">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Role}" />
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
              <DataGridTemplateColumn.CellEditingTemplate>
                <DataTemplate>
                  <ComboBox ItemsSource="{Binding AllowedRoles}" SelectedItem="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataTemplate>
              </DataGridTemplateColumn.CellEditingTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Name" Width="1.6*" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Address" Width="2*" Binding="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Volume" Width="90" Binding="{Binding Volume, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Folio" Width="90" Binding="{Binding Folio, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Lot" Width="90" Binding="{Binding Lot, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="LandVal No." Width="120" Binding="{Binding LandValNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <DataGridTextColumn Header="Exam No." Width="110" Binding="{Binding ExamNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>

      <TabItem Header="Spatial Units">
        <DataGrid Margin="8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding SpatialUnits}">
          <DataGrid.Columns>
            <DataGridTextColumn Header="parcel_name" Width="1.8*" Binding="{Binding ParcelName}" />
            <DataGridTextColumn Header="area_sqr" Width="1.2*" Binding="{Binding AreaSqr}" />
            <DataGridTextColumn Header="suid" Width="1.4*" Binding="{Binding Suid}" />
            <DataGridTextColumn Header="created_utc" Width="1.6*" Binding="{Binding CreatedUtc}" />
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>

      <TabItem Header="Plan Check">
        <Grid Margin="8">
          <DataGrid AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    ItemsSource="{Binding PlanCheckRows}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Check Type" Width="1.6*" Binding="{Binding CheckType}" IsReadOnly="True" />
              <DataGridCheckBoxColumn Header="Acceptable" Width="110" Binding="{Binding Acceptable}" />
              <DataGridTextColumn Header="Description" Width="3*" Binding="{Binding Description}" />
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
      </TabItem>

      <TabItem Header="Sources / Map Evidence">
        <Grid Margin="8">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Linked transaction sources and working_review query evidence." FontWeight="SemiBold" />
          <ListBox Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding SourceLabels}" />
        </Grid>
      </TabItem>
    </TabControl>

    <Border Grid.Row="3" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="0,1,0,0" Padding="10,6">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ProgressBar Grid.Column="0"
                     Width="64"
                     Height="8"
                     Margin="0,0,8,0"
                     IsIndeterminate="True"
                     VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
        <TextBlock Grid.Column="1"
                   Text="{Binding StatusText}"
                   TextWrapping="Wrap"
                   Foreground="#41515A"
                   VerticalAlignment="Center" />
      </Grid>
    </Border>
  </Grid>
</controls:ProWindow>


