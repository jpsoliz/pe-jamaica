<controls:ProWindow x:Class="ParcelWorkflowAddIn.FabricMaintenancePromotionWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
                    Title="Fabric Maintenance"
                    Width="980"
                    Height="720"
                    MinWidth="860"
                    MinHeight="620"
                    WindowStartupLocation="CenterOwner">
  <controls:ProWindow.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <Style x:Key="FabricIconImageStyle" TargetType="Image">
      <Setter Property="Width" Value="16" />
      <Setter Property="Height" Value="16" />
      <Setter Property="Margin" Value="0,0,6,0" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
  </controls:ProWindow.Resources>
  <Grid Background="#F4F7F9">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="#EAF2F7" BorderBrush="#C8D6DF" BorderThickness="0,0,0,1" Padding="10,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
          <TextBlock FontWeight="SemiBold" Text="Current Transaction:" />
          <TextBlock Margin="6,0,16,0" Text="{Binding CurrentTransactionNumber}" />
          <TextBlock FontWeight="SemiBold" Text="Parcel in Review:" />
          <TextBox Width="110"
                   Height="22"
                   Margin="6,0,16,0"
                   Text="{Binding ParcelInReview, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   IsEnabled="{Binding IsParcelInReviewEditable}" />
          <TextBlock FontWeight="SemiBold" Text="Target:" />
          <TextBlock Margin="6,0,0,0" Text="{Binding TargetLabel}" />
        </StackPanel>
        <TextBlock Grid.Column="1" Text="Review-only map context" Foreground="#41515A" />
      </Grid>
    </Border>

    <TabControl Grid.Row="1" Margin="8">
      <TabItem Header="Review And Decide">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <Grid Margin="8">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8" Margin="0,0,0,8">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Final Target" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                  <RadioButton Width="108" Height="24" Content="Legal" GroupName="FinalTarget" IsChecked="{Binding IsLegalTargetSelected, Mode=TwoWay}" VerticalContentAlignment="Center" />
                  <RadioButton Width="108" Height="24" Margin="8,0,0,0" Content="Cadastral" GroupName="FinalTarget" IsChecked="{Binding IsCadastralTargetSelected, Mode=TwoWay}" VerticalContentAlignment="Center" />
                  <Button Width="124" Height="26" Margin="16,0,0,0" Command="{Binding LoadParcelCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                      <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/LoadParcels.png" />
                      <TextBlock VerticalAlignment="Center" Text="Load Parcel" />
                    </StackPanel>
                  </Button>
                </StackPanel>
                <TextBlock Margin="0,8,0,0" Text="{Binding CandidateStatus}" TextWrapping="Wrap" Foreground="#53636A" />
              </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8" Margin="0,0,0,8">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Review Results" />
                <DataGrid Margin="0,8,0,0"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          ItemsSource="{Binding ReviewResults}"
                          MinHeight="92">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="1.1*" />
                    <DataGridTextColumn Header="Query Key" Binding="{Binding QueryKey}" Width="1.7*" />
                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="70" />
                    <DataGridTextColumn Header="Spatial Relation" Binding="{Binding SpatialRelationMode}" Width="1.3*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1.6*" />
                  </DataGrid.Columns>
                </DataGrid>
              </StackPanel>
            </Border>

            <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8" Margin="0,0,0,8">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Final Candidates" />
                <DataGrid Margin="0,8,0,0"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          ItemsSource="{Binding FinalCandidates}"
                          SelectedItem="{Binding SelectedFinalCandidate, Mode=TwoWay}"
                          MinHeight="98">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Candidate" Binding="{Binding CandidateId}" Width="1.3*" />
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="1.1*" />
                    <DataGridTextColumn Header="ObjectID" Binding="{Binding ObjectId}" Width="90" />
                    <DataGridTextColumn Header="Relationship" Binding="{Binding SpatialRelationship}" Width="1.1*" />
                    <DataGridTextColumn Header="Overlap %" Binding="{Binding OverlapPercent}" Width="90" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1.4*" />
                  </DataGrid.Columns>
                </DataGrid>
              </StackPanel>
            </Border>

            <Grid Grid.Row="3" Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8">
                <StackPanel>
                  <Expander Header="{Binding TopologyReviewSummary}"
                            FontWeight="SemiBold"
                            IsExpanded="{Binding IsTopologyReviewExpanded, Mode=OneWay}">
                    <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding ReviewChecks}">
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <TextBlock Margin="0,0,0,4" Text="{Binding Message}" TextWrapping="Wrap" FontWeight="Normal" />
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </Expander>
                  <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Width="136" Height="26" Command="{Binding SelectWorkingFeaturesCommand}">
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/SelectFeatures.png" />
                        <TextBlock VerticalAlignment="Center" Text="Select Features" />
                      </StackPanel>
                    </Button>
                    <Button Width="142" Height="26" Margin="8,0,0,0" Command="{Binding OpenAttributeTablesCommand}">
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/AttributeTables.png" />
                        <TextBlock VerticalAlignment="Center" Text="Attribute Tables" />
                      </StackPanel>
                    </Button>
                    <Button Width="134" Height="26" Margin="8,0,0,0" Command="{Binding OpenTopologyToolsCommand}">
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/TopologyRules.png" />
                        <TextBlock VerticalAlignment="Center" Text="Topology Tools" />
                      </StackPanel>
                    </Button>
                  </StackPanel>
                </StackPanel>
              </Border>
              <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8">
                <StackPanel>
                  <Expander Header="{Binding AttributeReviewSummary}"
                            FontWeight="SemiBold"
                            IsExpanded="{Binding IsAttributeReviewExpanded, Mode=OneWay}">
                    <StackPanel>
                      <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding AttributeChecks}">
                        <ItemsControl.ItemTemplate>
                          <DataTemplate>
                            <TextBlock Margin="0,0,0,4" Text="{Binding Message}" TextWrapping="Wrap" FontWeight="Normal" />
                          </DataTemplate>
                        </ItemsControl.ItemTemplate>
                      </ItemsControl>
                      <TextBlock Margin="0,8,0,0" Text="{Binding WorkingReviewPlan.Message}" TextWrapping="Wrap" Foreground="#53636A" FontWeight="Normal" />
                    </StackPanel>
                  </Expander>
                </StackPanel>
              </Border>
            </Grid>

            <Border Grid.Row="4" Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8" Margin="0,0,0,8">
              <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Decision" />
                <WrapPanel Margin="0,8,0,0">
                  <Button Width="150" Height="26" Margin="0,0,8,8" Command="{Binding ReplaceExistingCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                      <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/ReplaceExistingLayer.png" />
                      <TextBlock VerticalAlignment="Center" Text="Replace Existing" />
                    </StackPanel>
                  </Button>
                  <Button Width="190" Height="26" Margin="0,0,8,8" Command="{Binding KeepExistingDiscardWorkingCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                      <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/KeepExistingDiscard.png" />
                      <TextBlock VerticalAlignment="Center" Text="Keep Existing / Discard" />
                    </StackPanel>
                  </Button>
                  <Button Width="194" Height="26" Margin="0,0,8,8" Command="{Binding MergeUpdateAttributesOnlyCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                      <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/MergeAttributesOnly.png" />
                      <TextBlock VerticalAlignment="Center" Text="Merge Attributes Only" />
                    </StackPanel>
                  </Button>
                  <Button Width="126" Height="26" Margin="0,0,8,8" Command="{Binding SendBackForReviewCommand}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                      <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/SendBack.png" />
                      <TextBlock VerticalAlignment="Center" Text="Send Back" />
                    </StackPanel>
                  </Button>
                </WrapPanel>
                <TextBlock Margin="0,0,0,8" Text="{Binding DecisionLabel, StringFormat=Selected decision: {0}}" FontWeight="SemiBold" />
                <TextBox Height="78" Text="{Binding DecisionNotes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                <TextBlock Margin="0,8,0,0" Text="{Binding ReadinessText}" TextWrapping="Wrap" Foreground="#53636A" />
                <Button Width="174" Height="28" Margin="0,8,0,0" HorizontalAlignment="Left" Command="{Binding ApproveForFinalWriteCommand}">
                  <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/ApproveFinalWrite.png" />
                    <TextBlock VerticalAlignment="Center" Text="Approve For Final Write" />
                  </StackPanel>
                </Button>
              </StackPanel>
            </Border>
          </Grid>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Final Layer Write">
        <Grid Margin="8">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <Border Background="#FFFFFF" BorderBrush="#C8D6DF" BorderThickness="1" Padding="8">
            <TextBlock FontWeight="SemiBold" Text="Final Write Confirmation" />
          </Border>
          <TextBlock Grid.Row="1" Margin="8" Text="{Binding ConfirmationSummary}" TextWrapping="Wrap" />
          <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsConfirmFinalWriteRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
              <ProgressBar Width="170" Height="14" IsIndeterminate="True" />
              <TextBlock Margin="8,0,0,0"
                         VerticalAlignment="Center"
                         Text="Saving Fabric Maintenance..."
                         Foreground="#41515A" />
            </StackPanel>
            <Button Grid.Column="1" Width="166" Height="28" HorizontalAlignment="Right" Command="{Binding ConfirmFinalWriteCommand}">
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/ApproveFinalWrite.png" />
                <TextBlock VerticalAlignment="Center" Text="Confirm Final Write" />
              </StackPanel>
            </Button>
          </Grid>
        </Grid>
      </TabItem>
    </TabControl>

    <Grid Grid.Row="2" Margin="10,0,10,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <ProgressBar Width="120"
                     Height="12"
                     Margin="0,0,8,0"
                     IsIndeterminate="True"
                     Visibility="{Binding IsConfirmFinalWriteRunning, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Foreground="#41515A" VerticalAlignment="Center" />
      </StackPanel>
      <Button Grid.Column="1" Width="92" Height="28" Margin="12,0,0,0" Command="{Binding CancelCommand}">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <Image Style="{StaticResource FabricIconImageStyle}" Source="Images/FabricIcons/Cancel.png" />
          <TextBlock VerticalAlignment="Center" Text="Cancel" />
        </StackPanel>
      </Button>
    </Grid>
  </Grid>
</controls:ProWindow>
